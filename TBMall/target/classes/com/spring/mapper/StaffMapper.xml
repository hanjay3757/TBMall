<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mapper.StaffMapper">
	<!-- resultType 에 복수형(ArrayList 등)을 적는게 아님. 
	그냥 dto 단수의 경로를 적어야됨. -->
	<select id="getList" resultType="com.spring.dto.StaffDto">
		SELECT * FROM tbl_guest 
		WHERE deleted = 0
		ORDER BY bno DESC
	</select>
	
	<select id="read" resultType="com.spring.dto.StaffDto">
	select * from tbl_guest where bno = #{bno}
	
	</select>
	<update id="softDelete">
	UPDATE tbl_guest SET deleted = 1
	where bno = #{bno}
	</update>
	<update id="restore">
	UPDATE tbl_guest SET deleted = 0
	where bno = #{bno}
	</update>
	<select id="getDeletedStaff" resultType="com.spring.dto.StaffDto">
	 SELECT bno, btext, deleted
        FROM tbl_guest
        WHERE deleted = 1
	</select>
	<select id="login" resultType="com.spring.dto.StaffDto">
		SELECT * FROM tbl_guest 
		WHERE btext = #{staffId} AND password = #{password}
		AND deleted = 0
	</select>
	<update id="update" parameterType="com.spring.dto.StaffDto">
    UPDATE tbl_guest 
    SET 
        btext = #{btext},
        password = #{password},
        admins = #{admins}
    WHERE bno = #{bno}
</update>
</mapper>